# Production build settings (read automatically by react-scripts build).
# Committed intentionally (force-added past the .env* gitignore rule) so the
# Vercel build picks these up without dashboard config.

# Do not emit source maps to production — they expose full original source.
GENERATE_SOURCEMAP=false
